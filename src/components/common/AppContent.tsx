import './AppContent.css';
import Router from './Router';

const AppContent = () => {
  return (
    <main>
      <section>
        <Router />
      </section>
    </main>
  );
}

export default AppContent;